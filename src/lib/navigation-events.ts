/** Dispatched when the user chooses “Digital Skills” from the global nav while already on the hub route, so client state can return to the landing grid without a full reload. */
export const DIGITAL_SKILLS_NAV_ROOT_EVENT = 'examstitch:digital-skills-nav-root';
